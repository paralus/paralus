# Cluster Management and Scheduler

This module is responsible for manging clusters and bootstrapping them.


### Run cluster-scheduler server

``` shell
cd components/cluser-scheduler
go run main.go
```
