apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: rafay-connector
spec:
  template:
    spec:
      containers:
      - name: connector
        image: rafaysystems/rafay-connector:latest-1
        imagePullSecrets:
        - name: rcloud-registry-creds
        command:
        - test1
        - ""